import { makeStyles } from '@material-ui/core/styles'

const useStyles = makeStyles({
  root: {
    width: '100%',
  },
  link: {
    color: 'black',
  },
  header: {
    fontSize: 22,
    color: '#5c5855',
  },
  container: {
    maxHeight: '100%',
  },
})

export default useStyles
